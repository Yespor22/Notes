## 简单理解AI

### 从函数到神经网络
Function describe the world

- 符号主义
逻辑性，公式化
- 联结主义
近视求解

线形函数转换为非线性函数（拟和）

f(x)=g(wx+b) g(x)为非线性运算，也为激活函数

常用激活函数较为简单

$g(w_3g(w_1x_1+w_2x_2+b_1)+b_2)$ 

- 嵌套激活函数，个人理解为添加层数
- 输入层 -> 隐藏层 -> 输出层 （前向传播）

### 计算神经网络的参数

### 调教神经网络的办法

模型会遇到的其他问题：梯度消失，梯度爆炸，收敛速度，计算开销

相关解决方法：梯度裁剪，残差网络，权重初始化，归一化，动量法，RMSProp，Adam，mini-batch

### 从矩阵到CNN

### 从Embedding到CNN

### Transformer

